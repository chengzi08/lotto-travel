# 工作日誌

日期：2025-12-08

- **專案更名**：將專案名稱從「柏青哥旅行」更改為「妍妍子京都賭徒之旅」，並同步更新 HTML 標題、Manifest 設定與 Cache 名稱。
- **功能新增**：
    - **抽獎紀錄**：新增歷史紀錄功能，可查看抽籤時間、類別、預算與店名，並可單筆刪除。
    - **類別調整**：將「景點」類別更改為「購物」，對應圖示更新為 🛍️。
    - **特別任務**：當抽中「購物」時，會在進入輪盤前跳出「特別任務」提示框（搭配完拍出好看的五張照片）。
    - **滾動動畫**：實作拉霸機轉動時的圖示快速切換效果，提升視覺動態感。
- **文件新增**：建立 `README.md`，提供 GitHub Pages 部署教學。
- **Git 設定**：新增 `.gitignore` 檔案。
- **架構優化**：
    - 優化 `head`：新增 `theme-color`、Apple WebApp 相關 meta、`viewport-fit=cover`，改用 `<link>` 載入 Google Fonts。
    - 改善行為邏輯：移除所有 inline `onclick`，統一以事件監聽器綁定按鈕與返回鍵。
    - 加入 PWA 支援：新增 `manifest.json` 與 `sw.js`。
